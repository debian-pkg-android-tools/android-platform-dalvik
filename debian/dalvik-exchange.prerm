#!/bin/sh -e

set -e

#DEBHELPER#

case "$1" in
    (remove|deconfigure)
        update-alternatives --remove dx /usr/bin/dalvik-exchange
    ;;
    (upgrade)
    ;;

    (failed-upgrade)
    ;;

    (*)
        echo "prerm called with unknown argument \`$1'" >&2
        exit 0
    ;;
esac

exit 0
